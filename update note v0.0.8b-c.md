# 更新日志 v0.0.8 b 
**发布日期**：2026年8月27日  

---

## ✨ 全平台更新

### 🎨 界面与交互优化
- **导航栏重设计**  
  将顶部条状导航栏替换为**胶囊状**样式，视觉更紧凑圆润。

- **页面导航收纳**  
  所有页面导航入口统一收纳为**单个按钮**，点击后展开菜单，并新增**展开/收起的平滑过渡动画**，操作反馈更流畅。

- **滚动条自定义**  
  隐藏系统原生滚动条，替换为**本 Web UI 风格**的定制滚动条，与整体设计语言保持一致。

---

### 📂 课程表导入功能
- 新增从 **`.xls` / `.xlsx`** 文件导入课程表的能力。
- 导入时支持**选择是否保留原有课程表**（覆盖或合并）。
- 全面支持**单双周**课程显示逻辑。

---

### ⚙️ 性能与兼容性优化
- **WebDAV 页面调整**  
  因 WebDAV 需走服务器代理流量，原“网盘挂载仓库”页面现已改为 **“常用网盘/光网导航”** 页面，提升加载速度和实用性。

---

## 📱 移动端特性

- **毛玻璃特效动态关闭**  
  当页面宽度缩窄至移动端尺寸时，**自动关闭毛玻璃特效**，以减少渲染性能消耗，保证流畅滑动体验。

- **滚动条透明效果保留**  
  移动端同样保留滚动条的透明风格，与桌面端视觉统一。

- **鼠标接入时光效保留**  
  若移动设备外接鼠标，**伴随式移动光效**（悬停/点击高亮）仍会正常启用，兼顾桌面级操作习惯。

---



🔧 临时更新 v0.0.8c（底部补充）
本补丁在主体更新之上额外修复与新增以下内容：

移动端布局修复
修复了头顶胶囊导航条在移动端向右偏移的问题，现居中显示。

默认主题调整
首次进入网页时，系统将默认启用浅色模式，改善开箱体验。

课表卡片颜色自定义
在大课表中，您现在可以自由更改每门课程卡片的背景色，便于分类标识。

> 如有问题或建议，欢迎通过反馈渠道告知我。感谢使用！

---

# 更新日志 v0.0.8e（质量更新）
**发布日期**：2026年8月28日

本次为**质量更新**：重构交互细节、打磨动效手感、修复一系列体验问题，并对移动端渲染做了一次完整性能优化。

---

## ✨ 全平台更新

### 🎨 导航与交互
- **导航胶囊智能显隐**：鼠标靠近页面顶部时自动出现，离开后按滚动方向隐藏；显隐过渡加长，更柔和自然。
- **"变形生长"菜单**：目录按钮升级为从右上角弹性生长（`scale(0.3)→1` + 回弹缓动 `cubic-bezier(0.34,1.56,0.64,1)`），毛玻璃 `blur(28px)`，菜单项逐项错峰淡入，悬停轻微左移 + 背景高亮。
- **菜单内容整合**：立即咨询（CTA）、首页、关于我们、滚木服务、宿舍成员，并把**设置/清除本地背景**并入菜单（清除按钮仍仅在已选壁纸后出现）。
- **主题按钮与菜单按钮并排**：深浅色切换紧贴菜单按钮左侧，按压回弹效果一致；按任意按钮时胶囊背景联动"按下"反馈。
- **胶囊弹簧拖拽**：品牌胶囊与功能胶囊支持**长按拖拽、松手弹性归位**，采用对数弹簧阻尼（越拖越费劲、无硬墙）。
- 修复：拖拽时导航被滚动隐藏"缩回"、从品牌文字拖拽误触发"拖链接"、鼠标拖拽后不自动归位、胶囊在桌面端右缘错位等。

### 🪟 毛玻璃与伴随式光效
- 移除"液态玻璃"（SVG 位移折射）扭曲，回归**普通磨砂玻璃**，观感更干净。
- 伴随式光效修复"跨卡被拉扯"：位置即时贴合光标并保留**轻微拖尾**。
- 光效扫描改为**可重复执行**（初始 + 渲染完成后补扫 + 页面加载后补扫），由脚本动态创建的组件（如迷你课表每一行）也能稳定获得光效。
- 迷你课表行、标签条新增伴随光效与**悬停轻轻跃起**。

### 🃏 卡片动效
- 所有卡片（`.card` / 统计块 / 里程碑）悬停**轻轻跃起**，背景与阴影以同一弹性曲线柔和过渡。
- 修复跃起导致下方内容整体上移的问题（改用相对定位位移）。
- 交互元素（按钮/链接/胶囊等）禁用按住拖选文字。

### 📂 课程表
- **Excel 导入增强**：支持"网格课表"格式（节次 + 星期一~星期日、`★` 分隔字段、周次区间如 `1-16`/`9-16`、多个课程同格换行），与列字段格式自动识别；自动跳过顶部标题行定位表头；支持 **16 节次**显示。
- **课程卡片颜色自定义**：编辑/添加课程时可自由选择卡片背景色，也可恢复自动配色。
- **首页迷你课表**：自动滚动到"今天"的课程置顶；上下两端**渐变淡出**（无分割线）；移动端改为多行卡片布局，可读性提升。

### 🌗 主题与壁纸
- **跟随系统深浅色**：未手动选择时自动跟随系统/浏览器深浅色（`prefers-color-scheme`），并**实时响应**系统切换；手动切换后固定并保存，首屏无闪色。
- **深色主题默认壁纸**更换为"太空星空"（已压缩至约 0.5MB），浅色主题保持原默认壁纸；**两壁纸切换淡入淡出**（双层 opacity 过渡）。

### 🖱️ 自定义滚动条
- 替换为**覆盖式滚动条**：悬浮于背景之上、不占布局空间（不再挤压/偏移背景）。
- 滚动时渐显、停止后渐隐；鼠标靠近右侧区域自动出现；支持**点击跳转与拖拽跟随**；外观为黑灰磨砂玻璃风格。

---

## 📱 移动端特性（性能专项）

- **毛玻璃回归（省性能版）**：经性能 trace 分析（44s / 51 万事件）定位瓶颈为"滚动时模糊反复采样壁纸"：
  - 主要玻璃面板恢复 **6px** 小半径毛玻璃（卡片、hero、学期面板、标签条、统计块、里程碑、页脚），导航胶囊 12px；
  - 移动端去掉 `saturate/brightness`、小徽标/按钮保持实底、隐藏背景光斑、恢复屏外跳过渲染——兼顾流畅度与省电。
- 导航胶囊移动端收窄贴合内容、居中对称。

---

> 如有问题或建议，欢迎通过反馈渠道告知我。感谢使用！